/**
 * Basemap free para MapView.
 *
 * OpenFreeMap Liberty: tiles vectoriales sin API key ni tarjeta.
 * Misma URL que el sandbox de Fase 0. La atribucion la pinta MapLibre
 * (control por defecto) segun el style del proveedor.
 */
export const BASEMAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty'
